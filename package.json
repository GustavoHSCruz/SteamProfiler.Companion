{
  "name": "steamprofiler-companion",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Browser extension that adds SteamProfiler insights to Steam store pages.",
  "scripts": {
    "check": "node tools/check.mjs && node --test",
    "build": "npm run check && node tools/build.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
